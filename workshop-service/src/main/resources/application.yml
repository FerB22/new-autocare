server:
  port: ${PORT:8086}

spring:
  cloud:
    compatibility-verifier:
      enabled: false
  application:
    name: workshop-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/autocare_workshop}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:admin}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html